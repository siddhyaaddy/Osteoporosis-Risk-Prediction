
```{r}
# Importing required libraries
library(ggplot2)    # For data visualization (equivalent to matplotlib and seaborn)
library(dplyr)      # For data manipulation (similar to pandas)
library(tidyr)      # For data tidying
library(tibble)     # For working with tibbles (data frames)
```

```{r}
# Loading the dataset
df <- read.csv("osteoporosis.csv")
# Displaying the first few rows
head(df)
```

